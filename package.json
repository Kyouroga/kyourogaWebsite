{
  "name": "kyouroga-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "generate-routes": "tsx scripts/generate-routes.ts"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "vite": "^7.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
